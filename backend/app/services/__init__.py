"""Application business logic.

Each file in this folder owns one area of app behaviour (auth, claims,
assessor reviews, sanitization, storage UI helpers). Routers and pages
should import from here instead of putting that logic in the UI or API
layer.
"""

